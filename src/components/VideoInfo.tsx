import React from 'react';
import { Video } from '../types';

interface VideoInfoProps {
  video: Video;
}

const VideoInfo: React.FC<VideoInfoProps> = () => {
  return null;
};

const VideoActions: React.FC<VideoInfoProps> = () => {
  return null;
};

export { VideoInfo, VideoActions };